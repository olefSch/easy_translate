name: Deploy MkDocs Documentation

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build_and_upload_mkdocs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Setup uv
        uses: ./.github/actions/setup 

      - name: Install dependencies using uv sync
        run: |
          uv sync
        shell: bash

      - name: Deploy MkDocs
        run: |
          uv run mkdocs gh-deploy --config-file mkdocs.yml --site-dir _site
        shell: bash

      - name: Upload Github Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy_mkdocs:
    needs: build_and_upload_mkdocs
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

